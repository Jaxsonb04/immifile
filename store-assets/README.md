# Store assets

`app-store/6.9-inch/` — App Store Connect screenshots, captured on the
iPhone 16 Pro Max simulator (1320×2868, the 6.9-inch display size App Store
Connect requires). Status bar overridden to the standard 9:41 / full
signal via `xcrun simctl status_bar`. Retake after any visual redesign.
